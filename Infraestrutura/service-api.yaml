apiVersion: v1
kind: Service
metadata:
  name: fastfood-app-service
spec:
  type: NodePort
  selector:
    app: fastfood-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080 # Optional: specify a port or let Kubernetes assign one